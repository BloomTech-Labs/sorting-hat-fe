import React from 'rc-tree-select/lib/node_modules/react';
import { DataNode, Key } from 'rc-tree-select/lib/interface';
export interface TreeNodeProps extends Omit<DataNode, 'children'> {
    value: Key;
    children?: React.ReactNode;
}
/** This is a placeholder, not real render in dom */
declare const TreeNode: React.FC<TreeNodeProps>;
export default TreeNode;
