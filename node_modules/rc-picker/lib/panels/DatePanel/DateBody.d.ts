import * as React from 'rc-picker/lib/panels/DatePanel/node_modules/react';
import { GenerateConfig } from 'rc-picker/lib/generate';
import { Locale, OnSelect } from 'rc-picker/lib/interface';
export declare type DateRender<DateType> = (currentDate: DateType, today: DateType) => React.ReactNode;
export interface DateBodyPassProps<DateType> {
    dateRender?: DateRender<DateType>;
    disabledDate?: (date: DateType) => boolean;
    prefixColumn?: (date: DateType) => React.ReactNode;
    rowClassName?: (date: DateType) => string;
}
export interface DateBodyProps<DateType> extends DateBodyPassProps<DateType> {
    prefixCls: string;
    generateConfig: GenerateConfig<DateType>;
    value?: DateType | null;
    viewDate: DateType;
    locale: Locale;
    rowCount: number;
    onSelect: OnSelect<DateType>;
}
declare function DateBody<DateType>({ prefixCls, generateConfig, prefixColumn, rowClassName, locale, rowCount, viewDate, value, disabledDate, dateRender, onSelect, }: DateBodyProps<DateType>): JSX.Element;
export default DateBody;
